# Copy to .env and fill in. These are the BROWSER-SAFE keys (anon / publishable).
# The service-role key and the GitHub PAT are server-only secrets and must NEVER
# appear in this repo or the browser — see supabase/README.md.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-or-publishable-key
